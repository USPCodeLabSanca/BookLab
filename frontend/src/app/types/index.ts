//exemplo de tipagem (apenas para anotação)
export default interface tipo {
    nome: string,
    text: string,
    idade: Number
}

export interface Author {
    id: number;
    name: string;
    avatarUrl: string;
}
